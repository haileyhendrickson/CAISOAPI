# ask user for what report type they are looking for (XML Name. EX: PRC_LMP)
    # use an if statement to filter to the correct URL format
report_type = 'PRC_LMP' # ('What report are you looking for? Put in the code: ')

if report_type == 'PRC_LMP':
    # can group to use all nodes, or can specify one or multiple nodes
    print()
